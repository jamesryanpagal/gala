import { StyleSheet } from "react-native";

export const signup = StyleSheet.create({
  getStartedBodyContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  getStartedFooterContainer: {
    rowGap: 10,
  },
});
